{{template "base" .}}

{{define "title"}}tree{{end}}

{{define "content"}}
  {{template "header" .}}

  {{range .Data.Tree}}
    <div>
      <a href="{{.URL}}">{{.Path}}</a>
      <span> (lines: {{.NumLines}})</span>
    </div>
  {{end}}
{{end}}
