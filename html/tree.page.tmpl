{{template "base" .}}

{{define "title"}}tree{{end}}

{{define "content"}}
  {{template "header" .}}

  <div>
  {{range .Data.Tree}}
    <div class="flex justify-between mt">
      <div class="text-md">
        <a href="{{.URL}}">{{.Path}}</a>
      </div>
      <div class="mono">
        <span>lines: {{.NumLines}}</span>
      </div>
    </div>
  {{end}}
  </div>
{{end}}
