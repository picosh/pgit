{{template "base" .}}

{{define "title"}}log{{end}}

{{define "content"}}
  {{template "header" .}}

  <div>
  {{range .Data.Log}}
    <div class="box">
      <div>
        <a href="{{.URL}}">{{.SummaryStr}}</a>
      </div>
      <div>
        <span>{{.AuthorStr}}</span>
        <span>&nbsp;committed&nbsp;</span>
        <span>{{.WhenStr}}</span>
      </div>
    </div>
  {{end}}
  </div>
{{end}}
